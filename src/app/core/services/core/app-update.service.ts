import { Service } from '@angular/core';

/**
 * AppUpdateService — Service Worker removed.
 * SSR is incompatible with SW on the same origin;
 * SW has been removed in favour of SSR-driven fast first loads.
 */
@Service()
export class AppUpdateService {}